kind = "web"
previewPath = "/"
title = "Voyage — AI Travel Concierge"
version = "1.0.0"
id = "artifacts/voyage"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21665

[services.development]
run = "pnpm --filter @workspace/voyage run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/voyage", "run", "build" ]
publicDir = "artifacts/voyage/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21665"
BASE_PATH = "/"
